# Fake News Awareness Server
A server for a fake news awareness website

## Prerequsites
+ NodeJS
+ Postgres

## Setup
+ Setup the Postgres db tables using `db.sql`
+ Setup env variables as required in `db.js`
+ Run `node index.js` or `nodemon index.js`